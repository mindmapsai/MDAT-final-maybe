{
  "buildCommand": "npm run build --prefix multi-department-system && mkdir -p build && cp -r multi-department-system/build/* build/",
  "outputDirectory": "build",
  "installCommand": "npm install && npm install --prefix multi-department-system",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
