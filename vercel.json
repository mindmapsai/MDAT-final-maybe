{
  "buildCommand": "cd multi-department-system && npm install && npm run build && mkdir -p ../build && cp -r build/* ../build/",
  "outputDirectory": "build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
